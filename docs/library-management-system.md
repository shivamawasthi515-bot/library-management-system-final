# HLMS Implementation Notes

This repository implements the requested hybrid library system with:

- JWT auth and RBAC
- Books + borrowing + feedback
- Admin analytics and user management
- Local smart search (Mongo text index + fuzzy ranking)
- No external AI API key required by default
